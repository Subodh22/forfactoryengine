# The Turso database your engine syncs with (the SAME one). Server-side only —
# these never reach the browser. Set the same values in Vercel's project env.
TURSO_DATABASE_URL=libsql://your-db-name.turso.io
TURSO_AUTH_TOKEN=
